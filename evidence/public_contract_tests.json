{
  "passed": 11,
  "tests": [
    "English is default; locale change preserves numeric placeholders",
    "Unknown message and missing variables fail explicitly",
    "Reward settings affect score, not physical transition",
    "Normal contact force uses pole vertical acceleration, not fixed weight",
    "A failed-before-push trial is not a disturbance-recovery result",
    "Probe rejects invalid magnitudes, indices and duration",
    "Checkpoint refuses mismatched physics version even with a v2 envelope",
    "Checkpoint rejects nonnumeric curriculum metadata before it reaches HTML",
    "Checkpoint validates policy provenance separately from future training settings",
    "Checkpoint rejects prototype keys at every nesting level",
    "Checkpoint learning-statistic annotations must be numeric"
  ]
}